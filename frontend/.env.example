# URL of the FastAPI backend's predict endpoint
# Local dev default (used automatically if this file isn't present): http://127.0.0.1:8000/predict-segment
# For Docker/Render deployment, set this to your deployed backend URL
VITE_API_URL=http://127.0.0.1:8000/predict-segment
